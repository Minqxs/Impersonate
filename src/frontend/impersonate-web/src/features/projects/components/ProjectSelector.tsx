import { FormControl, InputLabel, MenuItem, Select } from '@mui/material';
import { useNavigate } from 'react-router-dom';
import { useQuery } from '@tanstack/react-query';
import { listProjects } from '../api/projectsApi';
import { useActiveProject } from '../ActiveProjectContext';
export function ProjectSelector(){const navigate=useNavigate();const{activeProjectId,activeProject,setActiveProject}=useActiveProject();const projects=useQuery({queryKey:['projects'],queryFn:({signal})=>listProjects(undefined,undefined,signal)});return <FormControl size="small" sx={{minWidth:{xs:0,sm:220},width:{xs:'100%',sm:220},maxWidth:220}}><InputLabel id="project-selector-label">Project</InputLabel><Select labelId="project-selector-label" label="Project" value={activeProjectId??''} displayEmpty disabled={projects.isLoading} onChange={event=>{const id=event.target.value;if(id==='all'){navigate('/projects');return;}setActiveProject(id);navigate(`/projects/${id}/dashboard`);}} renderValue={id=>!id?(projects.isLoading?'Loading projects…':'No project selected'):`${activeProject?.name??'Loading…'}${activeProject?` · ${activeProject.status}`:''}`}><MenuItem value="all">All projects</MenuItem>{projects.isError&&<MenuItem disabled>Projects unavailable</MenuItem>}{projects.data?.length===0&&<MenuItem disabled>No projects yet</MenuItem>}{projects.data?.map(project=><MenuItem key={project.id} value={project.id}>{project.name} · {project.status}</MenuItem>)}</Select></FormControl>}
