# Milestone 5 closure matrix

This matrix is based on implementation and automated-test evidence through `0227592`; roadmap labels are not treated as proof. Manual evidence is intentionally absent until all automated gates are Proven.

| Gate | Status | Implementation evidence | Automated-test evidence | Manual evidence | Remaining action | PR that closed the gate |
|---|---|---|---|---|---|---|
| 1. Core Coder and Reviewer loop | Proven | `PipelineRun`, `PlannedTask`, `TaskExecutionOrchestrator`, `CoderAgent`, and `ReviewerAgent` implement finite sequential coding/review/revision transitions, require repository inspection, a successful patch, and a real diff, and preserve attempts/reviews. | Domain transition tests, structured-output protocol tests, persistence mapping tests, and `CoreCoderReviewerLoopTests` prove fake-provider changes-requested → finite revision → approval, patch/SHA Reviewer input, superseded review history, false approval rejection, and safe failed terminal state. | No paid-provider runtime is required for negative-path closure; final live acceptance remains Gate 11. | None for Gate 1. | PR #17, hardened by #21–#23 and closed by PR #27. |
| 2. Incremental dependency patch composition | Proven | `TaskExecutionOrchestrator` selects only approved direct/transitive dependencies in deterministic task order; workspace preparation composes them into an index baseline, keeps the current-task diff incremental, and records source/dependency/tree/revision telemetry. The API projection now carries every persisted composition field. | PR #25 tests dependent/revision incremental diffs and safe pre-provider failures. `Three_task_dependency_chain_produces_apply_once_incremental_patches` proves P1 → revised P2 → P3 visibility, independence, apply-once behavior, and unchanged source HEAD/branches/worktree. `Pipeline_projection_includes_incremental_composition_telemetry` prevents null/zero projection regression. | No paid-provider or remote mutation is required; final live confirmation remains Gate 11. | None for Gate 2. | PR #25, completed by this PR. |
| 3. Provider capacity and bounded recovery | Partial | PR #26 parses capacity metadata, retries the same request, distinguishes quota, and coordinates process-local cooldowns. | Eleven fake-HTTP tests cover core parsing/classification/bounds. Cancellation, agent-specific preservation, fallback-after-exhaustion, and persisted/UI telemetry are not fully proven. | Not yet recorded. | Add missing fake-time/agent tests and persist/project retry telemetry after earlier gates merge. | PR #26 (partial). |
| 4. Generation- and specialisation-aware routing | Partial | Capability catalogue v2 distinguishes canonical family and variants and applies quality floors. | Existing router tests cover capability-aware behavior, but the full requested GPT-5-Codex/GPT-4.1 policy matrix and semantic-tie presentation require audit. | Not yet recorded. | Audit after Gate 3; create the prescribed routing PR only for actual gaps. | PR #24 (partial pending audit). |
| 5. Coder context and token efficiency | Partial | Provider rounds are separated from tools; read-only work is stopped after one compact correction; output limits are phase-aware; malformed completed output has one bounded repair. | Fake-provider tests prove early stop and Responses parsing for multi-item text, truncation, and refusal without paid calls. | Run `4e8de4ef-9376-4bb9-a8bc-19364bf9a725` consumed at least 108,308 tokens without a patch and remains the live reliability blocker. | Complete broader context-compaction acceptance and one bounded live confirmation. | This PR (partial). |
| 6. Shared repository cache and isolated worktrees | Missing | Every attempt still receives a fresh clone. | Workspace safety/composition tests exist, but cache/worktree reuse tests do not. | Not yet recorded. | Implement shared cache and detached worktrees in its prescribed focused PR. | — |
| 7. Workspace retention and storage controls | Missing | No configured lifecycle, quota, cleanup coordinator, storage summary, or maintenance operation exists. | No fake-time retention/cleanup tests. | Not yet recorded. | Implement after shared worktrees. | — |
| 8. State, failure and continuation semantics | Partial | Domain aggregate enforces sequential claims, retry exhaustion, continuation, skipping, and `ReadyForDelivery`. | Strong domain coverage exists; dependency eligibility and every final invariant still need consolidated integration acceptance. | Not yet recorded. | Close narrow defects found by final acceptance only after Gates 1–7. | PR #17 and later hardening (partial). |
| 9. Milestone 6 handoff contract | Missing | Approved artifacts contain much of the future input, but no complete read contract is defined. No Git delivery behavior exists. | No handoff projection contract test. | Not applicable yet. | Define/document the contract without implementing Milestone 6. | — |
| 10. Final automated acceptance | Blocked | Individual components exist. | Full backend validation passes (34 Domain, 44 Application, 42 Integration); frontend lint, 17 tests, and production build pass. The required three-task acceptance is absent. | Not applicable. | Complete prior gates, then run the exact three-task acceptance. | — |
| 11. Final manual acceptance | Blocked | Runtime surfaces exist. | Blocked until all automated gates are Proven. | No closure run recorded. | Apply migrations and perform one bounded live run only after Gate 10. | — |

## Current invocation decision

Gate 1 automated protocol correctness remains proven, but live Coder reliability is blocked by run `4e8de4ef-9376-4bb9-a8bc-19364bf9a725`: repeated no-patch provider rounds consumed at least 108,308 recorded tokens. Gate 3 remains Partial because broader provider-recovery evidence is outstanding. Gate 5 is Partial after the no-progress and compact-repair fixtures; broader context-compaction acceptance is still required. Milestone 5 is not complete.

Gate 2 was the first gate not Proven and is closed by the three-task temporary-repository acceptance plus the runtime projection correction on this branch. Gate 3 remains next and is deliberately not implemented here. This branch does not change provider retries, routing, context compaction, repository caching, retention, target-repository Git delivery, GitHub MCP delivery, or Milestone 6.
